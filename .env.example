# Process Blueprint — environment template.
# Copy to `.env` (gitignored) and fill in real values. NEVER commit real keys.
#   cp .env.example .env

# ===== LLM provider (Phase 2 brief) =====
LLM_PROVIDER=anthropic              # anthropic | openai | openrouter
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-opus-4-8
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o
OPENROUTER_API_KEY=
# OPENROUTER_MODEL=anthropic/claude-opus-4

# ===== Embeddings (Phase 3 knowledge retrieval) =====
EMBEDDINGS_PROVIDER=openai          # openai | fake
# OPENAI_EMBED_MODEL=text-embedding-3-small

# ===== Supabase (Phase 1.5/3 persistence + pgvector) =====
SUPABASE_URL=https://zrggqvgtthlhwbayckuc.supabase.co
SUPABASE_SERVICE_KEY=

# ===== Frontend (optional) =====
# If the backend is not on localhost:8000, set this in frontend/.env.local:
# NEXT_PUBLIC_API_URL=http://localhost:8000
