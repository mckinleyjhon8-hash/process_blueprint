# Process Blueprint — core dependencies
# Phase 1 (mining engine) is fully covered by pm4py + pandas.

# --- Phase 1: mining engine ---
pm4py>=2.7,<3
pandas>=2.0
numpy>=1.24

# --- UI / portal (Phase 4) ---
streamlit>=1.28
graphviz            # requires system Graphviz binaries for image rendering
Pillow

# --- Phase 2: LLM brief layer ---
langchain-core>=1.0
langchain-anthropic>=0.3    # Claude provider (default model: claude-opus-4-8)

# --- Dev / test ---
pytest>=8.0

# --- Later phases (uncomment when reached) ---
# supabase>=2.0           # Phase 3: persistence client (schema already applied)
