# Process Blueprint — core dependencies
# Phase 1 (mining engine) is fully covered by pm4py + pandas.

# --- Phase 1: mining engine ---
pm4py>=2.7,<3
pandas>=2.0
numpy>=1.24

# --- UI / portal (Phase 4) ---
streamlit>=1.28
graphviz            # requires system Graphviz binaries for image rendering
Pillow

# --- Phase 2: LLM brief layer (pluggable provider) ---
langchain-core>=1.0
langchain-anthropic>=0.3    # provider "anthropic" — default, claude-opus-4-8
langchain-openai>=0.2       # providers "openai" and "openrouter" (OpenAI-compatible)

# --- Phase 3: knowledge / persistence client (runtime only; tests don't need it) ---
supabase>=2.0           # SupabaseKB + persistence; schema already applied

# --- Phase 4: backend API (FastAPI serving the engine to the Next.js frontend) ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9  # required for file upload on /api/analyze

# --- Dev / test ---
pytest>=8.0
